@{ 
    ViewBag.Title = "UserDashBoard";

    <fieldset>
        <legend> Kullanıcı Paneli </legend>
        @if (Session["UserName"] != null)
        {<text>
                Hoşgeldin @Session["UserName"].ToString()
        </text>
    }
    </fieldset>
    
    }
